# Example environment variables for AI Memory project
# Token used by the API for authentication
# WARNING: replace the placeholder value below with your own secret token
AI_SECURITY__API_TOKEN=your-secret-token-change-me
# Privacy mode: "strict" stores minimal data (default), "standard" enables extra features
PRIVACY_MODE=strict
# Path to the SQLite database file
AI_DATABASE__DB_PATH=./data/memory.db
