name: CI

on:
  push:
    branches: [ main ]
    tags: [ 'v*' ]
  pull_request:

permissions:
  contents: read
  packages: write

jobs:
  lint-test:
    name: Lint & Test (Python ${{ matrix.python-version }} on ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: true
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: [ "3.11", "3.12" ]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ matrix.python-version }}-${{ hashFiles('**/pyproject.toml') }}-${{ hashFiles('**/poetry.lock') }}

      - name: Install dependencies (dev, editable)
        run: |
          python -m pip install --upgrade pip
          pip install -e .[dev]

      - name: Check for missing dependencies
        run: pip check

      - name: Ruff static analysis
        run: ruff check . | tee ruff.log

      - name: Upload Ruff log
        uses: actions/upload-artifact@v4
        with:
          name: ruff-log-${{ matrix.os }}-${{ matrix.python-version }}
          path: ruff.log

      - name: Mypy type checking
        run: mypy memory_system | tee mypy.log

      - name: Upload Mypy log
        uses: actions/upload-artifact@v4
        with:
          name: mypy-log-${{ matrix.os }}-${{ matrix.python-version }}
          path: mypy.log

      - name: Run tests with coverage
        run: pytest -q --cov=memory_system --cov-report=xml --cov-report=term -m "not perf" --junitxml=pytest-results.xml --durations=10

      - name: Show top 10 slowest tests
        run: |
          grep -A 10 "slowest" pytest-results.xml || true

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-xml-${{ matrix.os }}-${{ matrix.python-version }}
          path: coverage.xml

      - name: Upload pytest results
        uses: actions/upload-artifact@v4
        with:
          name: pytest-results-${{ matrix.os }}-${{ matrix.python-version }}
          path: pytest-results.xml

      - name: Pytest summary
        run: |
          tail -n 20 pytest-results.xml || true

      - name: Coverage summary
        run: |
          grep -A 10 "TOTAL" coverage.xml || true

      - name: Fail if coverage below 90%
        run: |
          coverage_percent=$(grep -oP 'line-rate="\K[0-9.]+' coverage.xml | head -1)
          percent=$(echo "$coverage_percent * 100" | bc)
          echo "Coverage: $percent%"
          if (( $(echo "$percent < 90" | bc -l) )); then
            echo "Coverage below 90%!"; exit 1
          fi
        shell: bash

      - name: Test summary
        run: |
          echo "Tests completed for ${{ matrix.os }} / Python ${{ matrix.python-version }}"

      - name: Cleanup pip cache (Linux only)
        if: runner.os == 'Linux'
        run: rm -rf ~/.cache/pip

      - name: Cleanup build artifacts
        run: |
          rm -rf .pytest_cache .mypy_cache

    continue-on-error: ${{ matrix.os != 'ubuntu-latest' }}

  docker-build-publish:
    name: Build & Publish Docker image
    runs-on: ubuntu-latest
    needs: lint-test
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile
          push: true
          tags: |
            ghcr.io/${{ github.repository }}:${{ github.ref_name }}
            ghcr.io/${{ github.repository }}:latest
